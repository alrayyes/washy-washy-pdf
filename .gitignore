# dependencies (bun install)
node_modules

# build output
dist
*.tgz

# generated by `bun run docs` (typedoc), deployed to GitHub Pages by CI
docs-site

# code coverage
coverage
*.lcov

# logs
logs
*.log
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# downloaded by bun run prose:sync — the Vale binary, and the style packages it
# lints against. styles/config is ours and stays committed, so the package
# directories are ignored by name rather than the whole styles/ tree
.tools
styles/Google
styles/proselint
